import { LetterChangePipe } from './letter-change.pipe';

describe('LetterChangePipe', () => {
  it('create an instance', () => {
    const pipe = new LetterChangePipe();
    expect(pipe).toBeTruthy();
  });
});
